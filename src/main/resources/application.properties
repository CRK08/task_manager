# Database Connection (Uses Environment Variables in Production, falls back to local MySQL in Dev)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/task_manager}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Kavi@3004}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT Configurations (Falls back to development secret)
jwt.secret=${JWT_SECRET:VGhpc0lzTXlWZXJ5U2VjcmV0S2V5Rm9yVGFza01hbmFnZXJBcHBsaWNhdGlvbjEyMzQ1Njc4OTAxMjM0NTY=}
jwt.expiration=${JWT_EXPIRATION:86400000}